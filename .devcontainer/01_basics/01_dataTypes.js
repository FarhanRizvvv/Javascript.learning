"use strict";//treat all JS Code as newer version of js//

//alert("hello ") // we are using nodejs, not browser
// code should be readable 
// tct9 ecma define the standard of js 
//for learning use mdn

let name="Farhan"// String ("" or '')
let age=18      /*Number (range 2^53)
                 if Number is very large then bigint will be use as datatype*/
let isLoggedIn //Boolean (True or False)
               // null is standalone value representation of empty value(value doent get to user)
               // undefined (when we doesnt assign any value to variable)
               //symbol use to identify the uniqueness of component

               //object
console.log(typeof"Farhan");
//console.log(typeof null); // o/p is object (interview)
//console.log(typeof undefined); o/p is undefined

