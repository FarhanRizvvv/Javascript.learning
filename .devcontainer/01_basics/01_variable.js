const accountId= 12345
let accountName= "Farhan"
var accountPassword= "1223"
accountCity= "Mumbai"
var accountCountry;
// Changing Const value is not allowed that's means we couldnt change it in ahead code//
 
/* let is used to remove redundancy in code , is define in block of scopr only
   means that we can change the value of same variable name of one code with same 
   variable name*/
   /* by default if we doesnt give the data type to variable name then it will 
   automatically refer it as a var , but it is bad to write as this*/
// if we doesnt assign any value then it will be undefined//
console.log(accountId)
console.table([accountId,accountName,accountPassword,accountCountry,accountCity])